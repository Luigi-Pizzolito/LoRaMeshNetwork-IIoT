class DBBridge {
    //Contructor to initialise database
    constructor(dbpath) {
        // this.db = new DB;
    }

    // Methods for accepting queries
    requestQuery(query) {

    }

    // Method for filtering DB entries
    filter(params) {

    }

    // Method for adding DB entries
    addEntry(entry) {
        
    }
};

module.exports = {
    DBBridge: DBBridge
};